// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

package frc.robot.commands;

 

/* 
import edu.wpi.first.math.controller.PIDController;
import edu.wpi.first.wpilibj2.command.Command;
import edu.wpi.first.wpilibj2.command.CommandBase;
import edu.wpi.first.wpilibj2.command.InstantCommand;
import edu.wpi.first.wpilibj2.command.SequentialCommandGroup;
import edu.wpi.first.wpilibj2.command.WaitCommand;
import frc.robot.Constants;
import frc.robot.subsystems.DriveSubsystem;
*/

//public class SimpleAuto extends  {
  /** Creates a new SimpleAuto. */
  /* 
  PathPlannerTrajectory M_movementPathMove = PathPlanner.loadPath("New Path", 8.0, 5.0);

  public Command followTrajectoryCommand(DriveSubsystem driveSubsystem, PathPlannerTrajectory m_examplePath) {
    //PathPlannerTrajectory examplePath = PathPlanner.loadPath("Example Path", new PathConstraints(4, 3));
    PathPlannerTrajectory movementPathMove = PathPlanner.loadPath("New Path", 8.0, 5.0);


    return new SequentialCommandGroup(
         new InstantCommand(() -> {
           // Reset odometry for the first path you run during auto
         }),
         //new PPSwerveControllerCommand(movementPathMove, null, null, null, null, null, isFinished(), null);
        new InstantCommand(() -> driveSubsystem.stop()),
        new WaitCommand(0.5)
      );
    
 }
 */
//}
